/**
 * External Links — ORGII Web Properties
 *
 * Centralizing the URLs here makes it easy to point all of them at a
 * future replacement domain.
 */

const ORGII_BASE_URL = "https://orgii.ai";

/** Hosted API key management — generate / rotate hosted_key tokens. */
export const ORGII_HOSTED_API_KEYS_URL = `${ORGII_BASE_URL}/account/api-keys`;
