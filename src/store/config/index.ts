/**
 * App configuration atoms (IDE prefs, chat appearance, internal UI editor paths).
 * Distinct from `settings/` (JSONC file); may derive from settings where applicable.
 */

export * from "./configAtom";
