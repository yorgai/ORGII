/**
 * Database Store
 *
 * Barrel export for database-related atoms and utilities.
 *
 * Database tabs live in the unified single `workstationLayoutAtom.mainPane`
 * pool and are accessed via `useWorkStationTabs()` like every other host.
 */

// Connection atoms and utilities
export * from "./atoms";
