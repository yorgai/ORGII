/**
 * Workspaces sidebar module entry.
 *
 * Re-exports the launchpad/workspaces tab sidebar component, which registers
 * itself in `TAB_SIDEBAR_REGISTRY` for launchpad repo tabs.
 */
export { WorkspacesTabSidebar } from "./WorkspacesTabSidebar";
