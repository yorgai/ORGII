/**
 * Workspaces sidebar module entry.
 *
 * Re-exports the launchpad/workspaces tab sidebar component (which
 * registers itself in `TAB_SIDEBAR_REGISTRY` at module-init for both the
 * `launchpad-dashboard` and `launchpad-repo` tab types).
 */
export { WorkspacesTabSidebar } from "./WorkspacesTabSidebar";
