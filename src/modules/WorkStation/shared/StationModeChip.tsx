import { memo } from "react";

import StationModePill from "./StationModePill";

export const StationModeChip = memo(StationModePill);
StationModeChip.displayName = "StationModeChip";

export default StationModeChip;
