/**
 * Output Content Types
 *
 * Re-exported from the canonical workstation types layer so CodeEditor
 * components can keep their existing import path.
 */
export type {
  OutputChannelType,
  OutputLine,
  OutputChannel,
  OutputPanelConfig,
} from "@src/types/workstation/output";
