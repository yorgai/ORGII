/** Max rows shown per list_dir event in the simulator file/search panel. */
export const SIMULATOR_LIST_DIR_DISPLAY_CAP = 250;

/**
 * Max rows parsed from a single event payload (memory guard for large directories).
 */
export const SIMULATOR_LIST_DIR_PARSE_SAFETY_CAP = 10_000;
