// Re-export from shared location — both SessionCreator and ChatPanel use the same hook.
export { useFileUpload } from "@src/hooks/useFileUpload";
export type { UseFileUploadOptions } from "@src/hooks/useFileUpload";
