/**
 * Shared composer voice-input UI primitives. Used by both the
 * ChatPanel InputArea and the SessionCreator EditorArea.
 */
export { default as VoiceInputButton } from "./VoiceInputButton";
export { default as VoiceRecordingBar } from "./VoiceRecordingBar";
