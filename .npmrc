# Use legacy peer deps resolution to avoid conflicts with older packages
legacy-peer-deps=true

# Speed up installs by using a local cache
prefer-offline=true

# Reduce npm output noise
loglevel=warn
