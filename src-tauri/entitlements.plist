<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation (needed for JavaScript) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dyld environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation (needed for plugins/sidecars) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client access -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Inherit entitlements for spawned processes -->
    <key>com.apple.security.inherit</key>
    <true/>
    
    <!-- File System Access - Like VSCode -->
    <!-- User selected files (read/write) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Downloads folder (read/write) -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
    
    <!-- Disable App Sandbox for full file system access like VSCode/Cursor -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!--
        Microphone access (hardened runtime).
        Required so the signed/notarized .app can call into
        AVCaptureDevice via WKWebView's getUserMedia. Without this the
        OS denies access even when NSMicrophoneUsageDescription is set.
    -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>

