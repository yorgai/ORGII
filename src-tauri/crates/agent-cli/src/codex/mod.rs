//! Codex CLI configuration module.
//!
//! Reads and writes `~/.codex/config.toml` — the user-level config for the
//! OpenAI Codex CLI agent.
//!
//! - **commands**: Tauri commands exposed to the frontend

pub mod commands;
