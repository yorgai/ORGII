//! Claude Code configuration module.
//!
//! Reads and writes `~/.claude/settings.json` — the user-level config
//! for the Claude Code CLI agent.
//!
//! - **commands**: Tauri commands exposed to the frontend

pub mod commands;
